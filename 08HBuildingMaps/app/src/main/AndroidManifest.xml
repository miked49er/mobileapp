<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.deiters.hbuildingmaps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CampusMap"
            android:label="@string/campus_title" />
        <activity
            android:name=".SecondFloor"
            android:label="@string/second_title" />
        <activity
            android:name=".ThirdFloor"
            android:label="@string/third_title" />
        <activity
            android:name=".About"
            android:label="@string/about_title" />
        <activity
            android:name=".FirstFloor"
            android:label="@string/first_title"></activity>
    </application>

</manifest>